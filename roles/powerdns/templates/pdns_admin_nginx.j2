server {
    listen 80;
    server_name admin.{{ ansible_fqdn }};

    {% if ssl_enabled %}
        listen 443 ssl;
        ssl_certificate      /etc/letencrypt/live/admin.{{ ansible_fqdn }}/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/admin.{{ ansible_fqdn }}/privkey.pem;
    {% endif %}

    location / {
        proxy_pass http://127.0.0.1:{{ powerdns.pdns_admin.port }};
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
    }
}